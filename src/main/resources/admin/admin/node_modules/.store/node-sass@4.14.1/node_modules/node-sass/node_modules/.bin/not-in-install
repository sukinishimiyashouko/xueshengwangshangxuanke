#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/C:/Users/11852/Desktop/1.15高校学生选课管理系统设计与实现/xueshengwangshangxuanke/src/main/resources/admin/admin/node_modules/.store/in-publish@2.0.1/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/C:/Users/11852/Desktop/1.15高校学生选课管理系统设计与实现/xueshengwangshangxuanke/src/main/resources/admin/admin/node_modules/.store/in-publish@2.0.1/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../in-publish@2.0.1/node_modules/in-publish/not-in-install.js" "$@"
else
  exec node  "$basedir/../../../../../in-publish@2.0.1/node_modules/in-publish/not-in-install.js" "$@"
fi
